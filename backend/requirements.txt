# Dependencias del backend / 后端依赖
# Núcleo (Fase 0) / 核心
fastapi==0.139.0
uvicorn==0.51.0
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings>=2.5
python-dotenv==1.2.2

# Autenticación (Fase 1) / 认证
bcrypt>=4.0
python-jose[cryptography]>=3.3
python-multipart>=0.0.9
email-validator>=2.0
GeoAlchemy2>=0.15

# Cifrado AES-256 (Fase 6, RNF-12) / AES-256 加密
cryptography>=41.0

# Pruebas (Fase 7) / 测试
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
pytest-asyncio>=0.23
factory-boy>=3.3

# Fases posteriores (se activarán cuando se necesiten) / 后续阶段（需要时再启用）
# pytest                       # pruebas
