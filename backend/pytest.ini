[tool:pytest]
# Reconoce clases de prueba en español / 识别西班牙语测试类
python_classes = Prueba* Test*
python_files = test_*.py
python_functions = test_*

# Coverage / 覆盖率
addopts = -v --tb=short -p no:cacheprovider
