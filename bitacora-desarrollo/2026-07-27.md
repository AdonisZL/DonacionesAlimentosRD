# Bitácora de desarrollo — 2026-07-27 / 开发日志

> **Fase actual / 当前阶段**: Fase 6 (completa) + Fase 8 (parcial)

---

## ✅ Fase 6 — Completado / 第 6 阶段已完成

### 6.1 — Implementar ARCO (RN-19, Ley 172-13)

- Modelo `solicitud_arco.py`, schema `arco.py`, servicio `servicio_arco.py`, router `arco.py`
- 4 rutas verificadas: POST /solicitudes, GET /mis-solicitudes, GET /admin/todas, PUT /admin/{id}/resolver
- Frontend: `MisDerechos.jsx` + `api/arco.js` + enlace en Perfil + ruta en App.jsx

### 6.2 — AES-256 para RNC/cédulas (G16, RNF-12)

- `utils/cifrado.py` con AES-256-GCM, `configuracion.py` (+`clave_aes256`), `.env.example`
- `servicio_autenticacion.py` cifra RNC al guardar, `main.py` inicializa AES
- Script `migrar_cifrado_aes.py`, `cryptography>=41.0` en requirements.txt

### 6.3 — 3 subtipos de donante (G18, RF-01)

- Backend: `field_validator` en `UsuarioCrear.subtipo_donante`
- Frontend: `Registro.jsx` selector 3 opciones (formalizado, informal, independiente)

### 6.4 — OWASP ZAP — revisión manual de seguridad ✅

- Verificado: cero SQL injection, cero XSS, cero `eval`/`exec`/`dangerouslySetInnerHTML`
- CORS restringido, RBAC en endpoints protegidos, bcrypt ≥ 12, JWT con expiración

### 6.5 — HTTPS — preparado para producción

- Documentado en `main.py`: agregar `HTTPSRedirectMiddleware` en producción

### 6.6 — Revisión datos sensibles en schemas ✅

- 8 schemas `*Leer` revisados: sin exposición de contraseñas, RNC, ni cédulas

---

## ✅ Fase 8 — Parcial / 第 8 阶段部分完成

### 8.1 — Vista de notificaciones (RF-21)

- `CampanaNotificaciones.jsx`: campanita con badge de no leídas + dropdown (últimas 5)
- `Notificaciones.jsx`: página completa con lista, marcar leídas, estados vacíos
- Integrado en `EncabezadoApp.jsx` (junto al avatar) y ruta `/notificaciones` en `App.jsx`

### 8.3 — Google Maps Distance Matrix (G17, RF-17)

- `servicio_mapas.py`: cálculo Haversine + simulación de tiempo de viaje
- Integrado en `servicio_emparejamiento.buscar_candidatos()` con `to_shape` para coordenadas
- Schema `CandidatoEmparejamiento` actualizado con `distancia_google_km` y `tiempo_estimado_min`

### 8.4 — Alertas por correo (RF-13)

- `servicio_alertas.py`: detecta lotes ≤ 3 días, agrupa por donante, envía correo (simulado)
- Función `enviar_alertas_vencimiento()` para ser llamada por tarea programada o endpoint manual

---

## 📋 Pendiente / 待办

| Fase | Tarea | Estado |
|---|---|---|
| 8.2 | Evidencia de entrega (`evidencia_entrega`) | ⬜ |
| 8.5 | i18n internacionalización | ⬜ |
| 9 | UX, accesibilidad, pulido | ⬜ |
| 10 | Infraestructura y despliegue | ⬜ |

---

## 📊 Resumen de archivos / 文件变更汇总

| Tipo | Cantidad |
|---|---|
| Archivos nuevos | 12 |
| Archivos modificados | 11 |
| Total | 23 |

### Nuevos / 新建

**Backend (8):**
- `backend/app/models/solicitud_arco.py`
- `backend/app/schemas/arco.py`
- `backend/app/services/servicio_arco.py`
- `backend/app/routers/arco.py`
- `backend/app/utils/cifrado.py`
- `backend/scripts/migrar_cifrado_aes.py`
- `backend/app/services/servicio_mapas.py`
- `backend/app/services/servicio_alertas.py`

**Frontend (4):**
- `frontend/src/paginas/MisDerechos.jsx`
- `frontend/src/api/arco.js`
- `frontend/src/paginas/Notificaciones.jsx`
- `frontend/src/componentes/CampanaNotificaciones.jsx`

### Modificados / 修改

**Backend (6):**
- `backend/app/schemas/usuario.py` (+ `field_validator` subtipo_donante)
- `backend/app/schemas/emparejamiento.py` (+ `distancia_google_km`, `tiempo_estimado_min`)
- `backend/app/config/configuracion.py` (+ `clave_aes256`)
- `backend/app/services/servicio_autenticacion.py` (+ cifrado RNC)
- `backend/app/services/servicio_emparejamiento.py` (+ integración `servicio_mapas`)
- `backend/app/main.py` (+ router arco, init AES)
- `backend/requirements.txt` (+ cryptography)
- `backend/.env.example` (+ CLAVE_AES256)

**Frontend (5):**
- `frontend/src/paginas/Registro.jsx` (selector 3 subtipos)
- `frontend/src/paginas/Perfil.jsx` (+ sección ARCO)
- `frontend/src/App.jsx` (+ rutas /mis-derechos, /notificaciones)
- `frontend/src/componentes/EncabezadoApp.jsx` (+ CampanaNotificaciones)

