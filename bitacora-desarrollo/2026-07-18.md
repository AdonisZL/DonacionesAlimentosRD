# 2026-07-18

## Fase actual / 当前阶段
**Fase 0 + Fase 1 (OE1) núcleo COMPLETADOS**, con UI rediseñada (estilo intex, solo español) y documentación de entorno. / 阶段 0 + 阶段 1 核心完成，界面已重做（intex 风格、纯西语），并有环境文档。

## Completado hoy / 今日完成
- Revisión de requisitos ([Descripcion.md](../Descripcion.md)) y del script SQL (22 tablas). / 审阅需求与 SQL 脚本。
- Confirmación de decisiones: MVP por fases, OE1 primero, BD guiada, servicios simulados, estilo CSS propio. / 确认决策（含界面用自写 CSS）。
- Infraestructura de documentación (CLAUDE.md, docs/es, docs/zh, bitácora). / 文档基础设施。
- **Fase 0 — Cimientos** / 阶段 0 地基：
  - Entorno verificado: PostgreSQL 18.4, PostGIS 3.6.2, Python 3.11.9, Node 22.14. / 环境验证。
  - Backend: `requirements.txt`, `.env` (+ `.env.example`), `.gitignore`, `config/configuracion.py` (pydantic-settings), `database/conexion.py` (SQLAlchemy). / 后端配置与连接。
  - Base de datos `donaciones_alimentos` creada; 22 tablas + semillas (4 roles, 4 perecibilidad, 10 categorías de alimentos). / 建库、建表、种子数据。
  - Scripts: `scripts/inicializar_bd.py`, `scripts/verificar_bd.py`, SQL en `basedatos/`. / 建库与验证脚本。
  - Endpoint `GET /api/roles` (models/schemas/routers `rol`) + CORS para el frontend. / 后端端点 + 跨域。
  - Frontend: `react-router-dom` + `axios`; `estilos/global.css` (paleta verde); `api/cliente.js`; página `paginas/Inicio.jsx`. / 前端地基。
  - **Verificación**: la página muestra los 4 roles leídos del backend y la BD (front → back → BD OK, con captura). / 验证三层连通（含截图）。
- **Fase 1 — OE1 (núcleo)** / 阶段 1 核心：
  - Backend: dependencias (bcrypt, python-jose, python-multipart, email-validator); `utils/seguridad.py` (bcrypt coste 12 + JWT); `utils/dependencias.py`. / 后端认证工具。
  - Modelo `Usuario` + schemas con política de contraseña RNF-15. / 用户模型与模式。
  - `services/servicio_autenticacion.py` + `routers/autenticacion.py`: POST /api/auth/registro, POST /api/auth/login, GET /api/auth/yo. / 认证服务与路由。
  - Frontend: `api/autenticacion.js`, `context/ContextoSesion.jsx`, páginas `Login.jsx` y `Registro.jsx`, cliente con token, rutas. / 前端认证。
  - Verificado / 已验证: registro 201, login 200 (JWT), /yo protegido 401 sin token, contraseña débil 422, sesión activa en Inicio (captura). / 端到端验证。
- **Rediseño UI + entorno (tarde)** / 界面重做与环境（下午）：
  - Documentación de entorno para el equipo: `README.md` + `docs/es|zh/07`. / 给搭档的环境文档。
  - Tailwind CSS v3 + sistema de diseño de intex (colores, fuentes Hanken/Inter, Material Symbols). / Tailwind + 设计系统。
  - Páginas SOLO en español: Login (pantalla dividida), Registro (asistente 2 pasos con rol + subtipo), Inicio (barra superior); componente `Marca`. / 页面纯西语重做。
  - Backend: `subtipo_donante` en el registro (RF-01). Verificado registro donante formal 201. / 后端子类型。

## Pendiente / 待办
- [ ] **Fase 1 — resto de OE1** / 阶段 1 剩余:
  - Campos por rol: donante formal/especial (RNC, perfiles_legales), receptor y banco (direcciones_sedes, capacidad, cadena de frío). / 各角色专属字段。
  - Verificación de correo (RF-06, simulada) y recuperación de contraseña (RF-05). / 邮箱验证与找回密码。
  - Bloqueo por 5 intentos fallidos (RF-30). / 失败锁定。
  - Edición de perfil (RF-07) y desactivación de cuenta (RF-08). / 资料编辑与账号停用。
  - Registrar consentimiento en `consentimiento_datos` (RF-31 completo). / 完整同意记录。
- [ ] (Opcional) limpiar archivos huérfanos `index.css` y `App.css`. / 清理模板孤儿 CSS。

## Notas / 备注
- Servicios corriendo / 服务地址: backend `http://127.0.0.1:8000`, frontend `http://localhost:5173`.
- Arranque / 启动:
  - Backend (en `backend/`): `venv\Scripts\python.exe -m uvicorn app.main:app --port 8000`
  - Frontend (en `frontend/`): `npm run dev`
- Contraseña de la BD en `backend/.env` (no versionado). / 数据库密码在 `.env`（不提交）。
