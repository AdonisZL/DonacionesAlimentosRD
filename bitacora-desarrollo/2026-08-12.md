# 2026-08-12

## Fase actual / 当前阶段
RF-13 — Alertas de vencimiento (≤ 3 días) por correo / 临期预警邮件

## Completado hoy / 今日完成
- [x] `configuracion.py`: nuevos campos SMTP (`smtp_host`, `smtp_puerto`, `smtp_usuario`, `smtp_contrasena`, `correo_remitente`) + propiedad `correo_activo`
- [x] `servicio_correo.py`: envío real por `smtplib` (biblioteca estándar, sin dependencias nuevas) con modo simulado automático si no hay credenciales
- [x] `admin.py` + `schemas/admin.py`: nuevo endpoint `POST /api/admin/alertas-vencimiento/enviar` (solo administrador) que dispara `enviar_alertas_vencimiento` y registra auditoría
- [x] `Admin.jsx` + `api/admin.js`: botón "Enviar alertas" en el panel de administración
- [x] `.env.example`: plantilla SMTP documentada
- [x] Pruebas backend: 27/27 pasan

## Pendiente / 待办
- [ ] Usuario: obtener credenciales SMTP (p. ej. Gmail con contraseña de aplicación) y colocarlas en `.env`
- [ ] Probar envío real con un correo de prueba
- [ ] (Opcional) Programar ejecución diaria con el Programador de tareas de Windows

## Notas / 备注
- Sin credenciales SMTP el sistema sigue en modo simulado (impresión en consola), sin romper nada.
- Se corrigió además el resumen vacío de `servicio_alertas.py` (falta `total_lotes`).
