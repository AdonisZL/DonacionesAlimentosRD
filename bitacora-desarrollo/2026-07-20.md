# 2026-07-20

## Fase actual / 当前阶段
**Fase 5 (OE5) COMPLETADA — ¡MVP COMPLETO!**: RBAC (RF-28), auditoría (RF-29) y panel administrativo (RF-32). Con las fases 0–5, los 32 RF están cubiertos. / **阶段 5（OE5）已完成 — MVP 全部完成**：RBAC + 审计 + 管理面板。

## Completado hoy / 今日完成
- **Login con imagen** / 登录页配图: `fotos/01.png` como fondo del lado izquierdo con superposición degradada. / 左侧背景图 + 渐变遮罩。
- **Backend — campos por rol** / 后端角色专属字段:
  - Instalado GeoAlchemy2; modelos `PerfilLegal` y `DireccionSede` (columna geográfica PostGIS). / 安装 GeoAlchemy2 + 两个模型。
  - `UsuarioCrear` extendido: `rnc`, `direccion_texto`, `latitud`, `longitud`, `capacidad_diaria_kg`, `tiene_cadena_frio`, `horario_atencion`; validación de RNC (11 dígitos). / schema 扩展 + RNC 校验。
  - `registrar_usuario`: transacción multi-tabla (usuarios + perfiles_legales + direcciones_sedes) con `WKTElement` para las coordenadas; router traduce `ValueError` a 409. / 事务多表写入。
- **Frontend — Registro paso 2** / 前端注册步骤2: campos dinámicos según rol (RNC para donante formal/receptor/banco; dirección + latitud/longitud + capacidad para receptor/banco; horario + cadena de frío para banco). Limpieza de payload antes de enviar. / 按角色动态字段。
- **Verificado** / 已验证 (`scripts/probar_registro_rol.py`): receptor 201; `perfiles_legales.rnc`=13112345678; `direcciones_sedes` con dirección, capacidad 150.5, cadena de frío y `POINT(-69.9312 18.4861)`; RNC inválido → 422.
- **Registro: doble contraseña + validación previa** / 注册确认密码与前置校验: campo "Confirmar contraseña" y validación de reglas RNF-15 + coincidencia antes de pasar al paso 2. / 进入步骤2前校验密码规则与两次一致。
- **RF-30: bloqueo por intentos fallidos** / 失败锁定: el login cuenta los fallos; a los 5 bloquea 10 min (HTTP 423). Verificado (`scripts/probar_bloqueo.py`): 401×5 + 423. / 5 次失败锁定 10 分钟。
- **RF-06: verificación de correo (simulada)** / 邮箱验证（模拟）: servicio de correo que imprime en consola; JWT de verificación (24 h); endpoint `GET /verificar-correo`; se envía al registrarse; página `/verificar-correo`. / 注册后发送验证链接。
- **RF-05: recuperación de contraseña (simulada)** / 找回密码（模拟）: token opaco SHA-256 en `tokens_recuperacion_password` (15 min, un solo uso); endpoints `/recuperar-password` y `/restablecer-password`; páginas + enlace "¿Olvidaste tu contraseña?" en login. / 令牌 15 分钟一次性。
- **Verificado** (`scripts/probar_correo.py`): verificar 200 + `email_verificado=True`; token inválido 400; recuperar 200; restablecer 200; login con nueva contraseña 200; reusar token 400.
- **RF-31 consentimiento + RF-07 perfil + RF-08 desactivación** / 同意记录 + 资料编辑 + 账号停用:
  - Modelo `ConsentimientoDatos`; el registro escribe en `consentimiento_datos` (tipo, versión, IP). / 注册写入同意记录。
  - Endpoints `PUT /perfil` (editar) y `POST /desactivar` (baja lógica); el login rechaza cuentas inactivas (403). / 编辑与停用端点；停用后登录 403。
  - Frontend: página `Perfil` (editar + desactivar), ruta `/perfil`, enlace en Inicio; `UsuarioLeer` incluye `telefono`. / 资料页。
  - Verificado (`scripts/probar_perfil.py`): registro 201; consentimiento `(tratamiento_datos_172_13, True)`; editar 200; desactivar 200; login tras baja 403.

### Fase 2 — OE2 Inventario y FEFO (RF-09…16) / 阶段2：库存与 FEFO
- **Modelos** / 模型: `CategoriaAlimento`, `CategoriaPerecibilidad`, `Producto`, `LoteInventario`, `Merma`, `HistorialEstadoLote`. / 六个 ORM 模型。
- **Esquemas** / 模式 (`schemas/inventario.py`): catálogos, `ProductoCrear/Leer`, `LoteCrear` (valida vencimiento no pasado), `LoteLeer` (con `ventana_dias`, `en_alerta`, `bajo_umbral`), `AjusteInventario` (motivo del catálogo), `HistorialLeer`. / 含 FEFO 计算字段。
- **Servicio** / 服务 (`servicio_inventario.py`): `calcular_ventana` (RF-11); `registrar_lote` con rechazo si ventana ≤ 0 (RF-14) + alta en historial (RF-16); `listar_lotes` orden FEFO ascendente (RF-12) enriquecido; `listar_alertas` ≤ 3 días (RF-13); `ajustar_inventario` solo banco → merma + historial (RF-15/16).
- **Router** / 路由 (`/api/inventario`): catálogos, `POST /productos`, `POST /lotes`, `GET /lotes` (FEFO), `GET /alertas`, `GET /lotes/{id}/historial`, `POST /lotes/{id}/ajuste`. Registrado en `main.py`.
- **Frontend**: `api/inventario.js` + página `Inventario` (banner de alertas, formulario de lote con creación de producto en línea, tabla FEFO con badge de ventana, panel de historial y ajuste solo para banco); ruta `/inventario` + enlace en Inicio. / 库存页 + 路由 + 首页入口。
- **Verificado** (`scripts/probar_inventario.py`): rechazo vencido 422 (RF-14); FEFO `[2,10,25]` ascendente (RF-12); 1 alerta ≤3 días (RF-13); ajuste banco 200 (20→15) con merma (RF-15); historial con 2 movimientos (RF-16).

### Fase 3 — OE3 Emparejamiento inteligente (RF-17…22) / 阶段3：智能匹配
- **Modelos** / 模型: `Emparejamiento`, `IaEjecucion`, `Notificacion`, `EntregaTransaccion`, `Retroalimentacion`. / 五个 ORM 模型。
- **Servicio IA simulada** / 模拟 AI (`servicio_ia.py`): genera una justificación narrativa determinista (no llama a Gemini real). / 确定性说明。
- **Servicio emparejamiento** / 匹配服务 (`servicio_emparejamiento.py`): `buscar_candidatos` con PostGIS `ST_DWithin`/`ST_Distance` (radio ≤ 75 km, RF-17), compatibilidad de cadena de frío (RN-07) y capacidad (RN-11); `crear_emparejamiento` + justificación IA persistida (RF-18); `confirmar` → lote reservado + límite 48 h (RN-12) + notificaciones a ambas partes (RF-19/21); `rechazar` libera el lote para reasignar (RF-20); `completar` crea entrega; `crear_retroalimentacion` 1–5 (RF-22).
- **Router** / 路由 (`/api/emparejamientos`): `POST /candidatos`, `POST /`, `GET /`, `POST /{id}/confirmar|rechazar|completar|retroalimentacion`, `GET /notificaciones`, `POST /notificaciones/{id}/leida`. Registrado en `main.py`.
- **Frontend**: `api/emparejamiento.js` + página `Emparejamientos` (seleccionar lote + radio, listar candidatos con distancia + justificación IA, sugerir, y gestionar matches con confirmar/rechazar/completar/calificar); ruta `/emparejamientos` + enlace en Inicio.
- **Corrección de esquema** / 修复: `ia_ejecuciones.tipo_ejecucion` era `VARCHAR(20)` pero `'justificacion_narrativa'` tiene 23 caracteres → ampliado a `VARCHAR(30)` (SQL fuente + `scripts/migrar_ia_ejecuciones.py` sobre la BD viva). / 列长度修复。
- **Verificado** (`scripts/probar_emparejamiento.py`): receptor lejano (~155 km) excluido del radio 25 km (RF-17); justificación IA presente (RF-18); match creado 201 a 1.8 km; confirmar 200 + 1 notificación (RF-19/21); completar 201 (entrega); retroalimentación 201 (RF-22).

### Rediseño de interfaz (estilo ImpactDR) / 界面重做
- **Utilidades de estilo** / 样式工具 (`estilos/global.css`): `.text-gradient`, `.glass-card`, `.bg-hero-glow`, `.hover-lift` y animaciones `animar-flotar`/`animar-aparecer`/`animar-brillo`. / 渐变、玻璃卡、悬停、动画。
- **Componentes reutilizables** / 复用组件: `EncabezadoApp` (barra fija con navegación Inicio/Inventario/Emparejamientos/Perfil + avatar + salir) y `PieDePagina` (pie con enlaces y datos legales). / 统一页头与页脚。
- **Página de inicio** / 首页: landing rico para visitantes (hero con imagen `01.png`, sección «problema», «solución» con 3 tarjetas, banda de estadísticas oscura, «cómo funciona» en 3 pasos, CTA final) y panel mejorado para usuarios con sesión (bienvenida + acciones rápidas + datos de cuenta). / 访客落地页 + 登录后仪表盘。
- **Páginas internas** / 内部页面: `Inventario`, `Emparejamientos` y `Perfil` ahora usan `EncabezadoApp` + `PieDePagina` para un aspecto consistente. / 统一外观。
- **Login y Registro refinados** / 登录/注册细化: panel lateral con imagen `01.png` + degradado verde + chip «Compromiso dominicano»/«Red de impacto» + tarjeta glass flotante; enlace «Inicio»; mostrar/ocultar contraseña; botones con `hover:shadow-lg`; animación de entrada. / 侧栏配图+渐变+徽章+玻璃卡；密码显隐；按钮动效。
- Verificado en el navegador (`http://localhost:5173`): landing renderiza con hero, tarjetas y secciones correctas. / 浏览器验证通过。
### Fase 4 — OE4 Reportería (RF-23…27) / 阶段4：报表
- **Modelo** / 模型: `ReporteConsolidado` (tabla `reportes_consolidados`, con `parametros_busqueda` JSONB). / 合并报表模型。
- **Servicio** / 服务 (`servicio_reportes.py`): `reporte_donaciones` con filtros fecha/donante/categoría/estado (RF-23); `reporte_inventario` FEFO reutilizando `servicio_inventario` (RF-24); `reporte_asignaciones` (emparejamientos completados + entrega, RF-25); `exportar_sheets` genera CSV + URL simulada y guarda el reporte (RF-26); `reporte_fiscal` con cadena de hash SHA-256 (`hash_anterior` → `hash`), inmutable + rectificación por versión (RF-27, solo banco/admin). / 报表逻辑 + 哈希链。
- **Router** / 路由 (`/api/reportes`): `GET /donaciones`, `GET /inventario`, `GET /asignaciones`, `POST /exportar-sheets`, `POST /fiscal`, `GET /`. Registrado en `main.py`.
- **Frontend**: `api/reportes.js` + página `Reportes` con pestañas (donaciones con filtros + tabla + totales, inventario FEFO, asignaciones, fiscal DGII con hash); enlace `bar_chart` en `EncabezadoApp` + tarjeta en el panel de inicio; ruta `/reportes`. / 报表页（多标签）。
- **Verificado** (`scripts/probar_reportes.py`): donaciones 200 (6 lotes, 300 kg); inventario FEFO ascendente `[2,10,19,20,20,25]`; asignaciones 200 (1); export Sheets 201 (URL simulada); fiscal #1 hash + `anterior=None`, fiscal #2 `hash_anterior == hash #1` (encadenado ✔).

### Fase 5 — OE5 Seguridad y Administración (RF-28/29/32) / 阶段5：安全与管理
- **RF-28 RBAC**: dependencia `requerir_roles(*roles)` en `utils/dependencias.py`; el router `/api/admin` exige rol administrador; el auto-registro como administrador se bloquea (403). / 角色依赖 + 管理员路由保护 + 禁止自注册管理员。
- **RF-29 Auditoría**: modelo `BitacoraAuditoria` + `servicio_auditoria.registrar()`; se auditan login, registro, ajuste de inventario, reporte fiscal y cambios de estado (quién/qué/cuándo/IP). / 审计日志。
- **RF-32 Panel**: `servicio_admin.metricas_panel` (kg rescatados, tasa de efectividad, distribución por perecibilidad, usuarios por rol, lotes por estado); gestión de usuarios (activar/suspender) y consulta de auditoría. / 管理面板指标 + 用户管理。
- **Frontend**: `api/admin.js` + página `Admin` (métricas + tabla de usuarios + bitácora); enlace `Admin` en `EncabezadoApp` solo si el rol es administrador; ruta `/admin` con guarda de acceso. / 管理页 + 条件导航。
- **Admin**: se crea con `scripts/crear_admin.py` (no por API). Credenciales: `admin.demo@ejemplo.com` / `AdminRD2026!`.
- **Verificado**: banco → `/api/admin/panel` **403**; admin → panel **200** (kg=350, efectividad=50%); usuarios 200; auditoría registra eventos; auto-registro admin **403**.
## Pendiente / 待办
- [ ] (Opcional / 可选) mapa real (Leaflet) para coordenadas; limpiar `index.css`/`App.css` huérfanos; refinamiento visual adicional de páginas. / 真实地图选点、清理孤儿 CSS、页面细化。
- [ ] (Opcional) integrar servicios externos reales (Gemini, Google Sheets, correo SMTP) cuando se disponga de claves. / 接入真实外部服务。

## Notas / 备注
- Servicios / 服务: backend `http://127.0.0.1:8000`, frontend `http://localhost:5173`.
- Imagen del login en `frontend/fotos/01.png` (importada en `Login.jsx`). / 登录图片位置。
- Coordenadas: por ahora se ingresan a mano (prellenadas con Santo Domingo); el mapa es un servicio simulado. / 坐标暂手动输入。
- Inventario: la clasificación de perecibilidad (RF-10) se hereda del producto; la ventana (RF-11) se recalcula al listar. / 易腐性继承自产品，窗口按查询实时计算。
